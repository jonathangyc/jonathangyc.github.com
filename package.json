{
  "name": "jonathanc.com",
  "author": "jonathangyc",
  "version": "2.0.0",
  "description": "My personal portfolio site through your eyes",
  "private": true,
  "dependencies": {
    "sass": "^1.26.8"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7"
  },
  "scripts": {
    "watch": "browser-sync start --no-notify --proxy 'http://localhost:4000/' --files '_site/**/*'",
    "build:css": "sass --no-source-map --style=compressed sass:stylesheets --watch"
  }
}
